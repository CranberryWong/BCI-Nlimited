# The current UI shows only the adaptive performance workflow by default.
# Set to true temporarily when the legacy portrait generator, per-track editor,
# legacy output panel, and legacy music-event table are needed for migration.
VITE_SHOW_LEGACY_UI=false
