# Local Melody Model

`latest.pt` and `model_config.json` are produced by
`scripts/music_training/export_model.py`. Model checkpoints are ignored by Git.
When the files are absent or PyTorch is unavailable, the dashboard
automatically uses the rule composer.
